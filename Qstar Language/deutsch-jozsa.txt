|| dj || (qreg qr)
pre {n > 0}
	circ qr ->
        H(qr[:-1])
        X(qr[-1])
        assert { after X }
        H(qr[-1])
        oracle(qr) # SEQ(qr,oracle)
        H(qr[:-1])
        return
pos {true}


| oracle | (qreg qr)
pre {n > 0}
	circ qr -> 
		for q in qr[:-1] {
			invariant {true}
			cnot(qr[-1],qr[q])
			assert {true}
		}
		assert { true }
		return
pos {true}