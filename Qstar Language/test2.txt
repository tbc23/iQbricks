|| main || (qreg qr)
pre {n>0}
    circ qr[4] ->
        with control qr[0] (reverse(qft(qr)))
        for i in qr {
            invariant{true}
            Y(qr[i])
            X(qr[i])
        }
        Z(qr[0])
    return
pos {vazio}

