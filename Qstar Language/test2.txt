|| main || (qreg qr)
pre {vazio}
    circ qr ->
        if (len(qr)==0) {
            assert { qr_size is zero }
            return
        }
        else {
            H(qr[0])
            assert { had assert }
            X(qr[0])
            assert { X assert }
        }
pos {vazio}
