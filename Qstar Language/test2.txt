|| main || (qreg qr)
pre {n>0}
    circ qr[4] ->
        if (n>0){
            H(qr[0])
            assert { true }
            X(qr[0])
            assert { true }
            aux1(qr)
            aux2(qr)
        }
        return
pos {vazio}

| aux | (qreg qr)
pre {precond}
    circ qr ->
        with control qr[:-1] (RZ(1,qr[-1]))
        Ph(2,qr)
        return
pos {postcond}
