|| main || (qreg qr)
pre {n>0}
    circ qr[4] ->
        with control qr[0] (toff(qr[1],qr[2],qr[3]))
        for i in qr[0..-1] {
            invariant{true}
            Y(qr[i])
            X(qr[i])
        }
        Z(qr[0])
    return
pos {vazio}

