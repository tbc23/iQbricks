|| main || (qreg qr)
pre {n>0}
    circ qr ->
        if (n>0){
            H(qr[0])
            assert { true }
            X(qr[0])
            assert { true }
            aux1(qr)
            aux2(qr)
        }
        return
pos {vazio}

| aux1 | (qreg qr)
pre {true}
    circ qr ->
        return
pos {true}
